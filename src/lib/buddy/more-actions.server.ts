/**
 * Additional Buddy actions register here (checklist, tasks, transfers,
 * meetings, email and notifications). Imported for its side effect by the chat
 * and actions routes so both see the same registry.
 */
export {};
